@{
    ViewBag.Title = "Home Page";
}
<style>
    #question-block div
    {
        margin: 10px 10px;
    }
    .center {
    width: 50%;
    margin: 0 auto; 
}
</style>

<div id="question-block" class="center">
    <div>верхняя панель</div>
    <div id="txtQuestion" class="text">Вопрос</div>
    <div><input id="inpAnswer" type="text" class="form-control" /></div>
    <div><button id="btnSubmit" class="btn btn-primary center">Проверить</button></div>
</div>

@section scripts {
<script type="text/javascript">
    function StartStudy() {
        var url = "@Url.Action("StartStudy")";
        $.ajax({ type: "POST", url: url, data: { groupId: 0},
            success: function (response) {
                console.log(response);
                $("#txtQuestion").text(response.Question.Text);
            }
        });
    }

    function NextStep() {
        var url = "@Url.Action("NextStep")";
        $.ajax({ type: "POST", url: url, data: { answer: $("#inpAnswer").val() },
            success: function (response) {
                console.log(response);
                $("#txtQuestion").text(response.Question.Text);
            }
        });
    }

    $(function () {

        $("#btnSubmit").click(function () {
            NextStep();
        });

        StartStudy();
    });
</script>
}